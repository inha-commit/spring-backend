spring:
  profiles:
    active: mysql

jpa:
  open-in-view: true
  show-sql: true
  hibernate:
    ddl-auto: create
  properties:
    hibernate.format_sql: true
    dialect: org.hibernate.dialect.MYSQL8INNODBDialect
